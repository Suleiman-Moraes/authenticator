language: java
jdk:
  - openjdk17

# Fazendo o build antes de instalar as dependências
install: true

script:
  - mvn clean install

notifications:
  email:
    on_success: never
    on_failure: always
